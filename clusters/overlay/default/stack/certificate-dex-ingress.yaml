apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dex-ingress
  namespace: istio-ingress
spec:
  dnsNames:
    - auth.bikeshed.dev
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  privateKey:
    algorithm: ECDSA
    rotationPolicy: Always
  secretName: dex-ingress
