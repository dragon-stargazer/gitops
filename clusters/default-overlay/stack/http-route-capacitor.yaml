apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: capacitor
  namespace: flux-system
spec:
  hostnames:
    - capacitor.bikeshed.dev
  parentRefs:
    - name: eg
      namespace: watcher
    - name: istio
      namespace: watcher
  rules:
    - backendRefs:
        - name: capacitor
          port: 9000
